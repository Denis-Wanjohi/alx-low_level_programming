#include "main.h"
/**
 * mul-multiplies two int and return the value;
 * @x: int to be multiplied to y
 * @y: int to be multiplied to x
 * Return: product of a and b
 */
int mul(int a, int b)
{
return (a * b);
}
