#include "main.h"
#include <ctype.h>
/**
 * _isdigit - checks if a character is a digit
 * @c: is the character to be checked
 * Return: Always 0 (Success)
 */

int _isdigit(int c)
{
if (isdigit(c))
{
return (1);
}
else 
{
return (0);
}
return (0);
}
